Welcome to the **Modal Project**, which features a hero section with a background image and a centrally positioned container. When the user clicks a button, a modal overlay appears with a blue color, introducing the modal content, which currently includes a heading and a close button. After clicking the button to display the modal, the user can close it, returning to the hero screen. This project demonstrates the implementation of a modal interface with a smooth transition effect.
